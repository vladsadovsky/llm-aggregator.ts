# Build Notes

- V2 LLM follow-up scope boundary (2026-08-05): `doc/plans/V2_Master_Roadmap.md` now records the design-review decision separately from the deferred full AI-metadata migration. Product-facing, narrowly scoped LLM quality work may be proposed independently; renderer decomposition and generic provider/settings/suggestion frameworks are deliberately non-gating until a concrete feature justifies each refactor. Manual documentation check added to `doc/dev_process/test_tasks_2026-08-05.md`. Documentation-only; no E2E run.

- Grok Q&A source (2026-08-05): added Grok as a selectable source for both new-Q&A and edit workflows. Manual check: open Add Q&A and Edit Q&A, select Grok (including its Alt+number shortcut in the relevant form), save, and confirm the source remains Grok after reopening. No E2E run.

- Review follow-up phases 1–3 (2026-08-05): QA create/update/delete no longer use generic retry because an IPC rejection can follow a durable main-process write. On any mutation error the renderer reloads the archive once and rethrows the original error, preventing accidental replay while converging visible state; stale selected Q&A ids are cleared on reload. Thread mutations now clone a candidate map, persist that map, then publish it only after save success, so failed create/rename/membership/reorder writes cannot remain in renderer state or leak into a later save. Moving a Q&A validates both source and destination before changing either; list UI shows a failure instead of a false success toast. `ElectronAPI` and every bridge payload now have one source in `electron/preload.ts`; `src/global.d.ts` only re-exports that contract and augments `Window`. Focused renderer regression tests cover ambiguous QA write failures, failed thread persistence, and stale move destinations. No E2E run (user-requested). Focused tests, typecheck, and production build passed; the broad Vitest run had 614 passing tests, while two unrelated Electron-dependent suites could not start because the local Electron binary was locked/corrupt during an attempted reinstall.

- Combined LLM review follow-up (2026-08-05): added `doc/dev_process/DesignReview_codex_llm_followup_for_release_2.2.md`, validating the Grok 4.5 P0-P3 feedback against the current implementation and merging it with the whole-codebase style/correctness review. Most LLM P0/P1 transport, cancellation, guard, settings/secrets, status, archive-lookup, and embed-failure caching work is confirmed implemented. Remaining recommended work is narrowed to bounded model context, disposition of the unsafe legacy AI Metadata write path, explicit tag-policy approval, renderer embed-readiness projection, mutation consistency, and shared contract cleanup; provider-core and generic settings/suggestion frameworks remain deferred as disproportionate. Repository convention: durable review reports under `doc/dev_process/` use `DesignReview_<author>_<purpose>_for_release_<version>.md`; record the report and its disposition in this single build-notes file. Documentation-only review; production TypeScript/Vite build passed. No e2e run.

- Design-review P0/P1 LLM hardening (2026-08-05): shared `httpJson` transport now bounds wall-clock timeout (45s), response size (16 MiB), and AbortSignal cancel for Anthropic/Ollama/Azure/self-hosted adapters; OpenAI SDK calls also honour cancel. Suggestion work starts a main-side job (`ai:beginSuggestionJob` / `ai:cancelSuggestionJob`) so QA title/tag and thread-title suggestions abort on unmount. `assertCanCallLlm` fails closed on missing keys/endpoints/disabled experimental providers before any network call. Settings draft validation honors experimental feature flags, requires Azure/self-hosted connection fields, and accepts `azureApiKey`/`selfHostedApiKey` secret ownership. Application Status maps API-key status via `apiKeyField` (including local providers). Embed readiness cache degrades semantic search/embedding generation after a failed probe for the current provider/model fingerprint. Focused unit tests cover transport, jobs, guard, embed readiness, settings draft, and adapter fake-fetch `arrayBuffer` support. No e2e run (user-requested). No push.
- Settings Save IPC clone fix (2026-08-04): Settings → Save was failing every time with a red toast and console `Object could not be cloned` because nested Vue reactive proxies (`experimentalFeatures`, `providerConnections`) were passed straight into `settingsSave`. Electron IPC uses structured clone, which rejects Proxies. Save now plain-clones via `toIpcPayload` before invoke and surfaces a caught error toast if persistence still fails. Focused unit test pins the proxy→clone failure and the strip path. Manual check: open Settings, toggle any experimental provider or edit an Ollama/Azure/self-hosted field, Save — success toast, no clone error. No e2e run (user-requested).

- V2 Phase 2.1/2.2/2.5 initial delivery (2026-08-04): Settings now surfaces opt-in Ollama, Azure OpenAI, and self-hosted OpenAI-compatible providers. Self-hosted endpoints retain a fully-qualified configured base route so multiple DGX-hosted AI services can coexist. LAN hosts are an exact trusted-host allowlist and require HTTPS; the temporary trusted-LAN HTTP setting is gated by a development-only experimental flag and is rejected by packaged builds. Provider construction re-checks the saved flag/capability, and Settings requires saving a changed connection configuration before its test call. Q&As expose **Suggest title & tags**, which opens a draft, asks the configured completion provider for structured candidates, and writes only after the user reviews and saves; strict-tag archives discard unknown suggestions and canonicalize known aliases. Selected threads have the same review-first title suggestion in Rename. Thread batch generation, thread tags, and workflow/confidence suggestions remain deferred. Focused provider/policy/IPC tests: 56 passed; production build passed. No e2e run (user-requested).

- P0 archive access scalability (2026-08-04): startup now opens the app shell after the small thread/settings records are available, then scans Q&A markdown files asynchronously in bounded batches with progress in the Q&A panel. The main process caches the validated archive snapshot by id and pre-normalizes text/tag search material once per snapshot, so individual Q&A reads are map lookups and debounced literal searches never re-enumerate, parse, or concatenate every archive file. Q&A writes update the cache; directory changes, reset, and transactional thread deletions invalidate it, and a write during an initial scan makes that scan retry before publishing. Manual check: launch against a large archive; confirm threads/settings are usable while the Q&A panel reports loading, Q&A create/search activate when loading completes, then repeat full-text/tag searches and open/edit/delete a Q&A. No e2e run (user-requested).

- Issue #35 new-thread input isolation (2026-08-04): `NewThreadForm.vue` now owns the draft title/tags and emits only create/cancel actions to `ThreadsPanel`. Typing no longer invalidates the archive-scale panel render that contains every thread row, tags, action buttons, and tooltips. The new global `doc/guides/PERFORMANCE_AND_SCALABILITY.md` defines the 5,000-thread / 30,000-QA target and requires near-constant interactive work, non-blocking long-running operations, and explicit hot-path validation. Manual check: with a large archive, open Add thread and type rapidly in both fields; input must stay fluid, Enter must create, and Escape/Cancel must discard the draft. No e2e run (user-requested).

- Threads/Q&A header alignment (2026-08-04): removed the redundant in-pane “LLM Aggregator” brand bar above Threads, so the Threads and Q&A panel headers begin at the same vertical position. The Threads header now mirrors the QA header with a count chip immediately after “Threads”; it reflects the currently visible (filtered) real threads. Manual check: open the archive with and without a thread filter and confirm aligned headers plus an updated Threads count. No e2e run (user-requested).

- V2 Phase 1 completion (2026-08-04): QA list full-text/tag search now highlights literal query matches in titles and question snippets (semantic results are intentionally not highlighted because their match is conceptual, not lexical). QAs have locally persistent favorites and a 25-item recently accessed history stored in renderer localStorage; list-header star/history buttons scope the current list, individual row stars toggle a favorite. QA creation warns (but does not block) when a normalized title already exists. Settings already had the remembered-metadata reset; the Electron app now keeps a native tray icon with Open and Quit actions. Manual check: search a term with regex punctuation and confirm literal safe highlighting; favorite/reopen/reload a QA and check its star/history placement; create a duplicate title and confirm the warning; use the tray icon to restore a minimized window and quit. No e2e run (user-requested).

- Unified archive inspection and repair (2026-08-02): replaced the separate Tools menu commands for Health Check, Find Duplicate Q&As, and Find Redundant Threads with one **Tools → Inspect & Repair Archive…** entry and one inspection surface. It scans/provider-displays duplicate QA records, redundant thread wrappers, and advisory health findings together, but keeps their repair policies distinct: the same QA id in multiple threads is an intentional shared record and never a duplicate; duplicate-record repair remaps every membership to a user-selected survivor before deleting redundant files; redundant-wrapper repair removes only thread records; semantic similarity, metadata gaps, and unresolved entries remain advisory. The focused repair dialogs are reachable only from the single inspection surface. Manual check: open Inspect & Repair Archive in an archive containing an Add-to-Thread shared QA, a true duplicate record, and redundant wrappers; confirm the shared QA is absent from duplicate repair, each genuine finding has the appropriate review action, and the Tools menu exposes no separate duplicate/redundant/health entry. Validation: full suite passed (599 passed, 1 skipped); focused ESLint and production build passed.

- Safe cascading thread deletion (2026-08-02): single and bulk thread trash actions now preview one fresh main-process archive snapshot, confirm exact selected/unshared/shared counts, delete QAs owned only by the complete selected set, and preserve shared QAs exclusively in surviving threads—ordinary deletion no longer creates new Unthreaded items. Apply recomputes the plan and rejects stale membership or candidate-content changes; complete redundant-wrapper groups and duplicate QA ids fail closed into the existing repair tools. The main-side operation stages QA files behind a durable manifest, atomically saves the complete thread map once, rolls files back on save failure, and completes or reverses an interrupted transaction before renderer startup. Lost IPC responses reconcile both renderer stores before reporting failure. Application Trash remains a Phase 3 roadmap capability; the current confirmed delete is irreversible after its recovery journal is finalized. Full suite: 598 passed, 1 skipped; focused eslint and production build passed. Manual check: create two threads with one exclusive and one shared QA, delete one, verify the exclusive QA disappears and the shared QA remains only in the survivor; select all redundant wrappers and verify deletion is blocked with repair guidance.

- Redundant-thread detection/repair + duplicate-cleanup durability (2026-08-02, issue #30): Tools → Find Redundant Threads detects non-empty thread wrappers with identical canonical QA membership, uses collision-free fingerprints, chooses a deterministic survivor, discloses metadata differences, and merges tags/time spans without touching QA files. Repair is main-side against a fresh snapshot and promotes one complete `threads.json` map atomically (including Merge All); stale scans fail closed, and multiple distinct `importSourceId` values are blocked for manual review. Tools → Find Duplicate Q&As now sends an explicit keep/remove mapping, validates the reviewed groups again, durably remaps every thread to each chosen survivor before deleting any duplicate QA file, preserves retryability after file-delete failures, and keeps its success/failure outcome visible after rescanning. Single/bulk QA delete confirmations disclose shared-thread impact. Added pure, service failure-injection, IPC, renderer-state, and user-visible dialog seam tests; aggregate coverage is 583 passed, 1 skipped; production build and eslint (source clean; pre-existing `.history` warnings only) passed. Manual check: open each Tools dialog against an isolated archive containing duplicated QAs and redundant thread wrappers; confirm the proposed survivors and metadata, merge, reopen both scans, and verify zero findings plus unchanged QA content/thread membership.

- V2 3.1 selection UX overhaul — highlight-based + full keyboard/select-all (2026-08-02, beta feedback): replaced the per-row checkboxes with **highlight-only** selection on both lists (standard file-manager pattern). Selected rows get a primary-tint background; the active/viewed row keeps its accent left border and reads distinctly (a stronger tint when it is also selected). Added the common selection semantics on both QA and thread lists: **Ctrl/Cmd-click** toggles, **Shift-click** ranges (left-click; right-click stays the context menu), **Ctrl/Cmd+A** selects all visible (list focused), an explicit **Select all** header button, **Shift+↑/↓** extends the range, **Space** toggles the focused row, **Esc** clears, and plain **↑/↓** moves focus and collapses to one (Explorer semantics). Right-click is the context menu only (never extends selection — that is left-click + modifier, per the Windows standard): right-clicking inside a multi-selection keeps it and shows bulk menu items (Move/Copy/Delete N for QA; Export/Delete N for threads), while right-clicking an unselected row selects just it. Selection stays independent of the active detail item and pruned to the visible list. Full suite 544 passed, 1 skipped; typecheck/eslint clean; verified in the browser dev renderer (highlight, select-all, plain/Ctrl click, no console errors).

- V2 3.1 bulk operations on both lists (2026-08-02): multi-select + a bulk-action toolbar on the QA list and the thread list, built on the 0.3 `useSelectionModel`. Each list gets a per-row checkbox (unobtrusive until hover/selected) plus Ctrl/Cmd-click (toggle) and Shift-click (range) on the row; selection is **independent** of the active detail item (`selectedPairId`/`selectedThreadId`) — plain click still views+selects one, modifier-clicks extend without changing the view — and is pruned to the visible list on any filter/reload/thread change so it never carries stale ids (virtual thread rows Unthreaded/Search get no checkbox). QA toolbar: Delete (mirrors single-QA delete — purges thread membership then deletes the file, behind the confirm dialog; **no undo**, per instruction), Move to thread (popup of threads; move when a source thread is active, else add), Add tag (applies one tag to all selected), Export (loops `exportQA`), Clear. Thread toolbar: Delete (confirm; QAs inside are not deleted), Export (loops `exportThread`), Clear. Full suite 544 passed, 1 skipped; typecheck/eslint clean. Verified in the browser dev renderer (both toolbars appear on selection, independent per list, no console errors). Bulk export is a per-item save-dialog loop — a future "export to folder" API would make large multi-exports smoother.
- V2 1.3 context menus on list items (2026-08-02): right-click menus via PrimeVue `ContextMenu`. QA list items (`QAListPanel`): Edit / Duplicate / Move to thread ▸ / Copy to thread ▸ / Delete — Edit/Duplicate/Delete reuse the existing selected-QA event flow handled in `QAContentPanel` (the menu selects the right-clicked item first), and Move/Copy use `threadStore` (`threadsNotContaining` builds the submenu; Move uses `moveToThread` when a source thread is active, else `addToThread`; Copy always `addToThread`; an empty submenu shows a disabled "No other threads"). Thread items (`ThreadsPanel`): Rename / Export / Delete, reusing `startRename`/`exportThread`/`confirmDelete`. The context menu acts on the single right-clicked item; bulk actions live in the 3.1 toolbar. Verified in the browser dev renderer (both menus open, correct items, no console errors); typecheck/eslint clean.

- V2 1.1 + 1.5 threads list filter/search/sort + collapsible tag box (2026-08-02): the threads list now has the QA list's filtering standard, with **independent** state so typing in one panel never affects the other. `threadStore` gains `nameFilter`, `dateFrom`/`dateTo` (inclusive createdAt calendar-day range), `sortBy` (`name` default | `recent` = updatedAt desc | `size` = items.length desc), and `contentResultIds` (a QA-id set from an archive-wide `qaStore.searchPairs` content search; a thread matches when any member QA is in the set). `filteredSortedThreadIds` applies tags + name + date + content then sorts; the base `sortedThreadIds` stays alphabetical because `QAEditor`'s thread picker depends on it. `ThreadsPanel` adds a compact filter bar (name/content mode, sort select, toggleable date-range row, clear-all) and wraps the tag-selector chips in a collapsible, localStorage-persisted "Tags" box (1.5) so a long tag list no longer blows out vertical space. Content search is debounced with a sequence guard so a slow response can't overwrite a newer one. 14 renderer-unit tests added (`threadFilters.test.ts`); QA-panel search is untouched. Verified in the browser dev renderer (filter bar renders, date toggle works, QA search bar unchanged).

- Markdown thread-tag roundtrip (2026-08-02): thread exports now write `thread_tags` in file frontmatter, the parser carries those tags separately from per-QA tags, and populated-thread creation persists them in the same durable `threads.json` save as membership. Previously thread membership could roundtrip correctly while all thread tags silently disappeared, making the imported thread seem absent whenever a tag filter such as `claude` remained active. Format, renderer-persistence, store-reload, and Electron import tests now assert the tags as well as ordered membership. A file whose header says `export_type: qa` remains intentionally unthreaded; QA filenames do not start with `thread_`.
- Account-archive thread idempotency (2026-08-01): a real Claude repeat-import trace proved QA deduplication was working (`109` origin-id matches, `0` new pairs) while `commitArchiveImport` nevertheless created `11` new threads unconditionally. Account-imported threads now persist a stable `<format>:<provider-conversation-id>` identity, reuse and extend that thread on later exports, and report reused threads separately from newly created threads. For archives imported before the identity field existed, an exact QA-membership match is reused and stamped; multiple already-existing copies are reported but not implicitly deleted. Regression tests cover both consecutive imports and adoption of a pre-fix thread.
- Import diagnostics (2026-08-01): focused privacy-safe tracing now follows both app-file and account-archive imports across parse output, QA id creation/reuse, populated-thread save, and post-reload membership verification. Trace records contain identifiers and counts but not question/answer bodies. Run with both `VITE_DEBUG_LEVEL=trace` and `LLM_AGG_DEBUG_LEVEL=trace` to capture renderer and Electron-main decisions under the shared `fileImportTrace`, `threadStore`, and `bulkImportTrace` tags.
- File thread round-trip durability + real invariant gate (2026-08-01): `.md` thread import no longer creates an empty thread and performs one full `threads.json` save per QA membership. `threadStore.createThreadWithItems` now creates the populated ordered thread in one durable map save; the renderer reloads both stores, verifies every newly created QA id is still a member before reporting success, exits virtual/unthreaded views, and selects the imported thread. The prior tests explained the repeated regressions: `qaExportImportRoundtrip.test.ts` ended at formatter→parser, while the Electron import test used a hand-written fixture and asserted only that the thread *name* appeared. The default Vitest suite now has an export→parse→commit→reload membership invariant plus a store one-save/reload test; the Electron suite now performs an actual export→import and asserts both copies have two persisted, distinct memberships, and the legacy fixture test also checks `threads.json` membership. The macOS E2E isolation guard now canonicalizes existing paths before exact equality so `/var` and `/private/var` aliases do not block the suite, while sibling/prefix paths remain rejected.
- Bulk file-import regression/feedback hardening (2026-08-01): `import:archiveCommit` now accepts the existing optional `includeDateInThreadNames` selection field. Phase 0's strict IPC schema had omitted it even though the renderer always sent the field and the import service consumed it, causing every CSV/ZIP bulk commit to fail before writing with `invalid-payload: 1: unrecognized_keys`. The zod-inferred selection contract is now the type source for renderer, preload, and main instead of three handwritten shapes, and a renderer seam test submits the real `BulkImportDialog` payload through that runtime schema; both booleans are accepted while non-booleans/unknown keys remain rejected. File selection/preview parsing now displays immediate indeterminate feedback, and bulk commit displays/locks an immediate preparing phase rather than waiting silently for the first pair-progress event. Electron 43 changed dialogs without `defaultPath` to always open Downloads, so file import now remembers the last successfully selected parent directory for the app session (including after a later cancel or parse failure) and supplies it explicitly; transport tests cover reuse and cancel retention. The renderer harness also installs an in-memory `localStorage` fallback when Node 26 exposes its experimental storage global as undefined, keeping the default `npm test` command usable on the supported `node >=22` range.
- V2 P0-J coordinator extraction (2026-08-01, plan §P0-J): the two testable composable cores that `App.vue` should delegate to. `src/composables/useCommandRegistry.ts` — one registry of `{id, label, shortcut?, run, isVisible?, disabledReason?}` with unique ids (duplicate throws); `dispatch(id)` is the single path the native menu, palette, and keyboard all use, so behaviour can't drift, and it runs a command only when it exists, is visible, and is enabled; `commands` (visible only), `filter(query)` over label+shortcut, `isEnabled`/`disabledReason` for feature-gating. `src/composables/useImportCoordinator.ts` — owns the bulk-commit lifecycle: subscribes to the progress stream only for the commit's duration and unsubscribes after (a late event can't resurrect stale progress once `close()`d), enforces a single active import, projects a thrown error to a safe message (never a raw object/stack), and optionally reloads the archive after success; api surface injected for testing. 13 renderer-unit tests added. **Deferred to the user-run integration/E2E gate:** the actual `App.vue` surgery — moving `appCommands` onto `useCommandRegistry`, moving bulk state onto `useImportCoordinator`, and removing the `llm:*` DOM-event bus (`llm:import-file`, `llm:import-shared-link`, `llm:new-thread`, `llm:show-all-qas`, …) in favour of typed store actions / composable methods / emits — plus the `accelerators.test.ts` parity extension. This is the deliberately-late coordinator refactor; the reusable cores land first so that surgery targets settled APIs.
- V2 P0-H settings draft state machine (2026-08-01, plan §P0-H): the testable cores for "settings as one validated draft". Main: `electron/services/settingsDraftService.ts` (deps injected, Node-tested) — `validateSettings` checks provider known/enabled/completion-capable, model present, tag soft≤hard, and data dir present before any write; `validateDataDirectory(path, deps)` is probe-only (unsafe-path incl. NUL/relative, not-found, not-a-directory, not-writable, else ok + an archive summary) and never activates the directory; `applyDraft({settings, secretUpdates, expectedRevision}, deps)` uses an optimistic `settingsRevision` (stable-JSON SHA-256, key-order independent) so a stale draft returns `stale-revision` without writing, validates shape + secret-field ownership, then commits settings-then-secrets with a compensating rollback — a secrets failure restores the prior settings (`write-failed`), and a failed rollback returns `needs-repair`. Renderer: `src/composables/useSettingsDraft.ts` (jsdom-tested) owns original vs draft, dirty-field tracking (deep-clones the input so Cancel writes nothing), the loading/editing/validating/applying/error/done phases, a monotonic catalog request race (a superseded provider's slow model response is discarded), `canSave` gating (dirty + not mid-catalog), and a double-submit guard around apply. 31 tests added (22 Node + 9 renderer). **Deferred to the user-run integration/E2E gate:** the IPC channels/handlers wiring these with real fs deps + the post-apply archive reload, `ai:testConnection` against the visible draft, and splitting `SettingsDialog.vue` tabs into components over the composable.
- V2 0.4 undo/command infrastructure (2026-08-01, plan §10, INV-UNDO): `src/composables/useUndoManager.ts` (Vue-free, so its core is unit-tested in Node) adds `createUndoManager({archiveNamespace, limit})`. An `UndoableMutation` is a forward `apply()` + reverse `revert()` pair carrying an archive namespace and optional entity signatures (`appliedSignature`/`revertedSignature` + a live `currentSignature()`). The manager runs one op at a time and blocks overlap/re-entrancy (`busy`); records history **only after a durable success** and clears the redo stack on any new command; keeps a bounded session-only history (default 100, oldest evicted); revalidates the archive namespace (`wrong-archive`) and the expected signature before undo/redo — a mismatch (external/manual edit) invalidates the whole history (`history-stale`) and requires reload; and on a `revert`/re-`apply` failure marks itself **tainted**, refusing all further dispatch rather than pretending the rollback happened. `clear()` (archive switch / reset / bulk import / reload) empties history and clears taint. 15 Node unit tests added (success/failed-invoke-absent, LIFO undo+redo, redo invalidation, limit eviction, both staleness paths, taint on revert and on re-apply, clear-recovers-taint, busy + re-entrancy, wrong-archive, nothing-to-undo/redo). **Deferred to the user-run integration/E2E gate:** wrapping the concrete edit/tag/move/thread mutations and *recoverable* delete (archive-local trash / main-generated tombstone — needs main-side support), the reactive Vue wrapper for `canUndo`/`canRedo`, and Ctrl/Cmd+Z/Y accelerator registration.
- V2 0.3 shared multi-selection model (2026-08-01, plan §9): `src/composables/useSelectionModel.ts` is one tested implementation of list selection for both the QA and thread lists — `selectedIds` (ordered), `primaryId` (active row), `anchorId` (Shift range endpoint), `selectedCount`, and `isSelected`. `handleClick(id, visibleIds, {ctrl, shift})` dispatches plain-click (select one, set primary+anchor), Ctrl/Cmd-click (toggle membership + move primary/anchor to the row), and Shift-click (contiguous visible range from the anchor, forward or backward, anchor preserved so successive Shift-clicks re-extend); `toggleCheckbox` toggles without disturbing primary/anchor; `selectAll(visibleIds)` and range both operate on the caller-supplied **visible/filter-matching** order only. `prune(presentIds)` drops selections gone after reload/filter/delete and nulls a vanished primary/anchor; `clear()` resets everything (call on archive switch). It is independent of the existing single active-detail refs (`selectedPairId`/`selectedThreadId`) and exposes read-only `ComputedRef`s, so existing single-item commands keep acting on `primaryId` only and nothing becomes implicitly bulk-destructive. 15 renderer-unit tests added (every modifier, both range directions, re-filtered range, checkbox, select-all incl. empty, prune keep/drop, clear, two-model independence). **Deferred to the user-run integration/E2E gate:** wiring the model into `QAListPanel.vue` / `ThreadsPanel.vue` (checkbox mode, selected-count affordance) and the platform-modifier E2E — the logic core is fully unit-tested here.
- V2 P0-D atomic promote — transient Windows rename retry (2026-08-01, INV-DATA): `atomicFile.ts`'s promote step now retries a same-directory `renameSync` a bounded number of times (5, with a 10ms×attempt synchronous backoff) when it fails with a transient Windows code (`EPERM`/`EACCES`/`EBUSY`/`EEXIST`) — the antivirus/search-indexer brief-handle case the plan's P0-D failure matrix calls out. A non-transient error (e.g. `ENOENT`) still fails immediately, and after the budget is exhausted the original target is left byte-identical exactly as before. This surfaced because the new two-project Vitest run (renderer harness) raised parallel FS contention enough to hit the rare flake once; hardening the production rename is the correct fix rather than masking it with test-level retries. 3 unit tests added (retry-then-succeed, give-up-and-preserve, no-retry-for-non-transient).
- V2 renderer test harness (2026-08-01, plan §14.1): Vitest is now two projects under one aggregate `npm test` — `vitest.workspace.ts` defines "unit" (Node, `tests/unit/**`, unchanged) and "renderer" (jsdom, `tests/renderer/**`, with `@vitejs/plugin-vue`). `vitest.config.ts` is now a minimal root (so Vitest resolves its root here, not `vite.config.mts` with its Electron plugins) and the `test`/`test:watch` scripts drop `--config` so the workspace is auto-detected. `tests/renderer/setup.ts` installs a fresh active Pinia, a fresh typed fake `window.api` (reusing `src/api-mock.ts`'s `mockApi`, so renderer tests exercise the real `ElectronAPI` surface), and a clean DOM/localStorage before each test, restoring spies after. New devDeps: `jsdom`, `@vue/test-utils`. `tests/renderer/harness.smoke.test.ts` proves jsdom + @vue/test-utils mounting/reactivity + Pinia + the fake api all work. This unblocks the renderer-side Phase 0 groups (0.3 selection, 0.4 undo, P0-H settings draft, P0-J coordinators). Note: running two projects in parallel raised contention enough to surface a rare, pre-existing transient Windows `renameSync` EPERM in the atomic-write promote (hardened separately in the next entry).
- Pre-push line-ending guard (2026-08-01): `scripts/check-line-endings.mjs` + `npm run check:line-endings` (wired into `npm run check`) and `npm run fix:line-endings`. `.gitattributes` (`* text=auto eol=lf`) already normalizes tracked text files to LF in the index on commit and pins the working tree to LF, so per-commit manual EOL checks are unnecessary; this guard is the pre-push belt-and-suspenders. It fails if any tracked file whose **index** copy is LF has drifted to CRLF/mixed in the **working tree** (the churn-prone case), and `--fix` rewrites those to LF — a zero-index-churn correction. Committed-CRLF files (`i/crlf`, e.g. `.gitignore`) are deliberately left alone; normalizing them is a separate decision, not this guard's job. Binaries are skipped.
- V2 0.5 pure query/filter engine (2026-08-01): `shared/query/queryEngine.ts` (dependency-free of Electron/Pinia/DOM, like `shared/featureFlags.ts`) adds a serializable query AST — source set, tag set with explicit `all`/`any`, inclusive UTC date-day range, URL presence, and normalized full text — validated by a strict `zod` `QuerySchema` with bounds (text ≤1000, ≤50 sources, ≤100 tags, ≤64/term, `YYYY-MM-DD` dates, `from ≤ to`, unknown fields rejected, empty arrays rejected in favour of omission). `compileQuery(query)` compiles once into a pure predicate over a read-only `QueryablePair` projection (QAPairData is structurally assignable); an empty query is the identity filter. Semantics are pinned: matching uses NFKC normalization + locale-independent case folding and substring/equality only — **no user string is ever turned into a RegExp** (no ReDoS surface, tested with pathological input); a date filter compares the pair's UTC calendar day and a missing/invalid timestamp never matches; `hasUrl` treats whitespace-only urls as empty. `evaluateQuery` returns matching ids in a deterministic stable order (timestamp desc, id-ascending tiebreak, invalid timestamps last). Persistence of saved filters / virtual threads stays out of Phase 0 — these are pure parse/compile/evaluate functions for Phase 3 to consume. The existing feature-specific `searchService` is intentionally left unmigrated (the optional characterization-gated migration is deferred). 31 unit tests added (validation bounds, every predicate + AND combination, tag all/any, UTC date days, Unicode/case/punctuation, ordering determinism, a reference-evaluator property check, and the no-regex/ReDoS guard).
- V2 0.2 generic batch-LLM job runner (2026-08-01): `electron/services/llm/batchRunner.ts` extracts the batch/validate/collect pattern out of `annotationService` into a reusable, provider-injected core (INV-LLM/INV-COST). `runBatchJob(spec, items, provider, {signal, onProgress})` chunks by `batchSize`, checks the `AbortSignal` at the top of each batch (so cancel stops *before* the next batch, never mid-durable-write), calls the already-metered `CompletionProvider` exactly once per batch, strips only a known leading/trailing markdown fence (`stripJsonFences`), validates raw text through the spec's `parseResponse`, and rejects `unknown-id`/`duplicate-id` records — collecting per-item/per-batch errors without trusting model text. A provider throw or unparseable batch is isolated (that batch is skipped, the pass continues → partial success); generation writes nothing. Application is the separate `applyBatchProposals(targets, currentHash, apply)` command: it writes only where the target's current content still matches the hash captured at generation time (`hashContent` = SHA-256), reporting `applied` / `skippedStale` / `vanished` / `failed` so a changed or deleted target is skipped rather than clobbered, and per-item write failures never abort the batch. `annotationService.generateAnnotations` is migrated onto the runner as the proof consumer (now cancellable via an optional `AbortSignal`, using `getCompletionProvider()` and a strict `zod` per-record schema instead of a bare `JSON.parse` cast). 22 unit tests added (`batchRunner`, `batchApply`). Deferred/tracked (need the jsdom renderer harness, §14.1): the owner/TTL job registry (`batchJobRegistry`), the `batch.ts` IPC contract, and the generic review shell (`BatchReviewDialog.vue` + `useBatchReview`) with the pre-fan-out cost estimate and end-to-end annotation review/apply E2E — annotation apply still uses the existing IPC path (its `contentHash` is unused for now).
- V2 0.1 Ollama + Azure OpenAI adapters (2026-08-01): two capability-declaring adapters land behind their default-off flags (`localOllamaProvider`, `azureOpenAiProvider`), not wired into the live factory/UI yet (Phase 2.5 owns user-facing selection). `llm/localEndpointPolicy.ts` (pure) enforces loopback-only for Ollama — SSRF guard rejecting non-loopback IPv4 (incl. 10./192.168./public), DNS names, credentials, and non-HTTP schemes, normalizing to origin so no configured path is carried. `llm/azureEndpointPolicy.ts` (pure) requires HTTPS, an approved `*.openai.azure.com` / `*.cognitiveservices.azure.com` host, no credentials/port/fragment, normalized to origin. `ollamaProvider.ts` (complete+embed+local, zero-cost) and `azureOpenAiProvider.ts` (complete+embed) both take an injectable fetch, meter one usage-ledger entry per physical call, and map connection/404/auth/rate-limit/malformed-JSON failures to clear messages. 19 unit tests added (loopback + Azure endpoint matrices, adapter contract with a fake transport). Live integration remains opt-in/user-run (no live paid-provider tests in the suite).
- V2 0.6/0.7 feature flags + usage ledger (2026-08-01): `shared/featureFlags.ts` (dependency-free) declares the known experimental flags with metadata; `isFeatureEnabled(map, id)` is the single resolver both runtimes call — default-off, unknown ids fail closed but are preserved for forward compat. `AppSettings.experimentalFeatures` (id→boolean, bounded in the zod contract) persists them; `electron/services/featureFlags.ts` enforces in main independently of the renderer (`assertFeatureEnabled` throws the new `feature-disabled` code). `electron/services/llm/usageLedger.ts` (0.7, INV-COST) is one metered sink: call counts + token totals keyed by capability/provider/model/local, with an approximate cost from a dated public-pricing table (unknown model → "cost unavailable", local → zero) and `estimateBatchCost` for pre-fan-out warnings. It records no prompt/response text, ids, urls, or secrets. `tokenTracker` is now a thin facade over the ledger (IPC `ai:getTokenStats` shape unchanged), and both the OpenAI and Anthropic providers record one entry per physical call (Anthropic completions were previously unmetered). New IPC error code: `feature-disabled`. 16 unit tests added. Deferred/tracked: the Settings → Experimental Features UI and surfacing session usage in Application Status (renderer wiring for a later group); per-call failure/cancel metering and the per-session hard-limit override.
- V2 P0-I LLM capability split (2026-08-01): the universal `LLMProvider` is split into declared capability interfaces `CompletionProvider` / `EmbeddingProvider` (INV-LLM), so requesting an embedding no longer traps at runtime inside a completion-only provider's `embed()`. Registry descriptors now carry an immutable `capabilities: { complete, embed, streaming, local }` (OpenAI: complete+embed; Anthropic: complete only), exposed to the renderer via `aiListProviders` so the UI can gate features by capability rather than provider name. `providerFactory` adds `getCompletionProvider()` and `getEmbeddingProvider()` — the embedding factory validates the `embed` capability BEFORE constructing or loading a key, throwing a clear "does not support embeddings" message instead of letting Anthropic's `embed()` throw deep in a call; `embeddingService` now uses it. `getProvider()` is retained for completion consumers. `providerSupports(id, capability)` is the pure check (unknown → false). 12 unit tests added (providerRegistry, providerFactory). Deferred within G5 and tracked as remaining Phase 0 work: the `@anthropic-ai/sdk` migration (P0-I step 5), the Ollama/Azure adapters (0.1), the generic batch runner (0.2), and the centralized session usage ledger/guard replacing `tokenTracker` (0.7). `tokenTracker` is unchanged for now.
- V2 P0-G secret hardening + verified legacy migration (2026-08-01): the safe-storage backend now rejects Linux `basic_text` (INV-SECRET) — `SafeStorageCrypto` gained an optional `getSelectedStorageBackend()`, and on Linux a `basic_text` selection is treated as unavailable for both read and write (a stable `SAFE_STORAGE_INSECURE_BACKEND` warning; save throws) rather than being presented as encrypted storage. The encrypted envelope is now written through the P0-D atomic primitive with last-known-good and a decrypt-before-promote validator, so a save that cannot be read back never replaces a good envelope. Startup no longer only orphans `secrets.json` as cleartext: `secrets/legacyMigration.ts` reads the known keys, merges them into encrypted storage WITHOUT overwriting a valid stored value, persists, reloads and compares keyed SHA-256 fingerprints, and only then unlinks the plaintext (and any prior `.orphaned.bak`). If secure storage is unavailable, a file is unparseable, or verification fails, the plaintext is left untouched and a warning is surfaced (`LEGACY_FILE_ORPHANED` / `LEGACY_MIGRATION_FAILED`); a successful run reports `LEGACY_MIGRATED` and notes that file deletion cannot guarantee erasure from SSD wear-leveling or cloud-sync history. `initSecretsStorage` now runs the migration instead of the rename-only sweep (`legacyCleanup.ts` is retained for its constants and remains unit-tested). No secret value is ever logged. 21 unit tests added/updated (Linux basic_text, atomic decrypt-before-promote, and the full migration matrix). Manual Linux keyring-vs-basic_text and Windows install/restart/upgrade checks remain user-run.
- V2 P0-E/F import containment + bulk durability (2026-08-01): shared-link transport is now bounded and contained (INV-NET). `import/remoteUrlPolicy.ts` (pure) requires HTTPS, no credentials, the default port, and an exact registered API host, and redacts the final path token in logs/errors. `conversationFetcher.fetchJson` does manual redirect handling — every hop is re-validated against the host allowlist, capped at 3 hops, a 20 s overall timeout, and a 16 MiB decoded-body ceiling — surfacing structured codes (invalid-url, redirect-disallowed, redirect-limit, timeout, response-too-large, http-error, format-changed) that `sharedLinkImportService` maps to coded IpcErrors so friendly messages survive the bridge; share tokens are no longer logged. The Gemini hidden window now uses a unique non-persistent partition, denies popups and all permission requests on its own session, and blocks navigation off the Gemini render hosts (a sign-in redirect for a private/expired link is denied and yields no content). Bulk import (INV-IMPORT): preview ids are `crypto.randomUUID()`, previews expire on a 10-minute unref'd TTL sweeper, and a commit claims its preview (rejecting a double commit). `commitArchiveImport` is now async and cancellable — it takes an AbortSignal, treats each conversation as the durable unit (writing its pairs then saving threads.json atomically before moving on), yields to the event loop every 64 pairs so main stays responsive, stops before the next durable unit on cancel without promoting a half-written thread (its pairs remain recoverable on re-run via origin_id), and returns a `cancelled` flag. `import:archiveCancel` now aborts an in-flight commit rather than only dropping the preview. Deferred within P0-F (tracked): preview owner/webContents binding, selection by an opaque previewThreadId (still keyed on provider sourceId), and shard-manifest byte/count budgets. 31 unit tests added (remoteUrlPolicy, fetchJsonPolicy via a fake net, bulkImport cancellation/incremental-save); the live-provider smoke and bulk-import cancel E2E remain user-run.
- V2 P0-C canonical IPC contracts + sender guard (2026-08-01): every privileged channel now flows through one validated registrar (`electron/ipc/registerValidatedHandler.ts`). Channel strings are centralized in `shared/contracts/channels.ts` (dependency-free, imported by preload/main/tests) and each channel has a canonical `zod` argument schema in `shared/contracts/*`; the `channelArgs` registry uses `satisfies Record<Channel, …>` so a channel added without a schema fails the build (compile-time parity). Before any service runs, the wrapper rejects untrusted senders (`INV-IPC`: `event.sender` must equal the main window's webContents, `event.senderFrame` must be its `mainFrame`, and the frame origin must match the app origin) and parses the argument tuple (strict objects, bounded strings/arrays, finite numbers, traversal-shaped ids rejected — `INV-PATH`). Failures cross the bridge as stable coded errors (`invalid-sender`, `invalid-payload`, `not-found`, …) with no stack, path, secret, or provider body (`INV-OBS`); unexpected throws are logged main-side only and surface as a generic `internal` code. Success return shapes are unchanged, so the renderer is untouched except that `preload.ts` now routes through channel constants and decodes coded errors into clean `Error` objects carrying `.code`. New prod dependency: `zod`. Per-domain user-facing error messages (import/secret/LLM) are hardened to specific codes in their own later groups; until then a service throw shows the generic message. 48 unit tests added (`ipcSenderPolicy`, `ipcContracts`, `ipcRegistrar`). Remaining manual/E2E gate: an iframe/unexpected-frame privileged-call rejection E2E (deferred to the user-run suite).
- V2 P0-A/P0-B baseline and runtime upgrade (2026-07-31): clean installs now verify the lockfile-resolved imported-content parser chain (`markdown-it`, its `linkify-it`, gray-matter's `js-yaml`, and Vite's `postcss`) through `npm run check:dependencies`; parser budgets warm up and use repeated median scaling checks in addition to absolute ceilings, and Markdown file imports have a 64 MiB runtime limit. CI uses Node 24 with clean `npm ci && npm run check` gates on Windows and Apple Silicon macOS plus Electron smoke jobs on both. Electron is upgraded from 33.4.11 to the latest stable 43.2.0 and electron-builder to 26.15.3; `console-message` uses the current details object. Supported packaged targets are Windows 11 x64 and macOS 12+ Apple Silicon (ARM64, no Intel artifact). Focused smoke coverage verifies production-bundle renderer launch, preload presence, QA create/read round trip, clean teardown, and denial of remote navigation/popups; native file dialogs are covered through unit stubs. `@types/yauzl` is now direct because application archive code had incorrectly depended on builder 25's transitive declaration package. The required production audit is clean; a full dev-tree audit still reports six Vite/Vitest-family advisories whose fixes require coordinated Vite 8/Vitest 4 majors and are outside the isolated Electron/builder upgrade.
- Release branching policy (2026-07-31): `release/1.x` is the long-lived v1 maintenance line, initialized from the immutable `v1.4.1` tag. `vlads-dev` carries active v2 work; merge tested v2 milestones into `master`, which must remain stable and releasable. Ship v1-compatible fixes from short-lived branches into `release/1.x`, then cherry-pick them to `vlads-dev` only when still relevant. See `doc/guides/RELEASE_BRANCHING.md`.
- Debug logging policy (2026-07-26): app debug traces use a numeric level model (`TRACE=0`, `DEBUG=1`, `WARNING=2`, `ERROR=3`) with inclusive threshold filtering. `npm run dev` now auto-initializes `VITE_DEBUG_LEVEL=debug` when launched from the VS Code integrated terminal. The logger defaults to `ERROR` in **every** build — dev and production — when no explicit level override is provided, so real failures are always visible; an explicit level or the legacy boolean flag lowers the threshold. `npm run dev:debug` remains available for intentional verbose runs.
- Logger implementation (2026-07-26): level parsing, resolution, and the four log functions are a single source of truth in `shared/logCore.ts`. `electron/services/logger.ts` and `src/utils/logger.ts` are thin wrappers that only supply their env sources (`process.env` vs `import.meta.env`) — do not fork the level logic back into them. `parseLogLevel`/`resolveConfiguredLevel` are unit-tested in `tests/unit/logLevel.test.ts`. `debugTrace`/`debugWarn` exist but are not yet called anywhere (reserved for future use).
- TODO (uiStore zoom): `loadZoom(key = ZOOM_KEY)` in `src/stores/uiStore.ts` takes a storage-key parameter, but no caller passes one and the zoom setters still hardcode `ZOOM_KEY`. The parameter is kept for a planned per-surface zoom (separate content vs. other panes); finish the refactor by threading a key through the setters/callers or drop the parameter if that direction is abandoned.
- Shared-link import architecture research (2026-07-25): Google's Gemini Help currently documents public conversation links as `https://g.co/gemini/share/<id>`; observed direct links also include `https://share.gemini.google/<id>`, while `https://gemini.google.com/share/<id>` is a legacy form. The short-term compatibility patch accepts the observed direct host in addition to the legacy path, but URL-prefix matching is not a durable provider contract and does not cover the documented short URL.
	- Proposed direction: replace the import service's exact-host `detectProvider(rawUrl)` gate with a two-stage pipeline: first parse and normalize the pasted value with the standard `URL` API, then resolve an approved short-link alias and classify the resulting canonical URL through a shared provider registry. Each provider adapter owns URL matching, share-reference parsing where needed, and provider-specific import. Gemini's rendered-page importer receives the validated canonical URL directly and must not depend on extracting a Gemini share ID; ChatGPT and Copilot keep deterministic ID parsing for their JSON API import paths.
	- Security policy: accept known aliases permissively but do not make the hidden Gemini `BrowserWindow` an unrestricted browser. Require HTTPS, reject credentials, non-default ports, malformed and local/private-network targets, cap redirects, record the redirect chain, and permit only registered provider/shortener destinations. During Gemini rendering, use Electron `will-navigate` and `will-redirect` to block unapproved main-frame navigation and deny `window.open`; retain sandboxing, context isolation, and disabled Node integration. Electron exposes the needed redirect/navigation events and final URL inspection through `webContents`.
	- Diagnostics: return structured failures (`invalid-url`, `unsupported-provider`, `redirect-disallowed`, `private-or-expired`, `format-changed`) rather than the current generic unrecognized-link message. Redacted development logging may record unknown redirects from otherwise trusted providers without retaining public-link tokens.
	- Test matrix: table-drive direct, legacy, and official-short Gemini forms; mocked redirect resolution; redirect-to-untrusted-host rejection; retained ChatGPT/Copilot routes; malformed URLs, credentials, non-default ports, and private-IP rejection; and Gemini rendering with a canonical URL but no parsed share ID.
	- Delivery stages: first add `g.co/gemini/share/...` through controlled resolution and update the UI/documentation to say “public share link” with examples rather than fixed supported-host copy. Then introduce the provider registry/resolver with the security policy and migrate ChatGPT, Copilot, and Gemini adapters. Do not transform a Gemini URL by hand: resolve it, validate the final provider destination, then render that canonical location.
- Settings and command follow-up (2026-07-25): Ctrl/Cmd+K now opens the command palette even from a focused input. View retains two zoom groups: Content Zoom commands for the selected Q&A and Electron's standard Zoom In/Out/Reset Zoom commands for the whole window. Archive maintenance actions are in Tools; Application Status is in Help. In Settings → AI, Lens and development-key controls share a left edge, the redundant missing-OpenAI-key catalog message is hidden, and Re-check secure storage matches outlined secondary controls. The tag-dictionary count aligns with the tag-limit controls.
- Settings status cleanup (2026-07-25): Settings suppresses the redundant no-key and legacy-plaintext-file messages while retaining actual secure-storage failures. The secure-storage re-check is a labeled action on its own line, and the Metadata & Tags tab labels the live tag-dictionary count. README documents that development environment variables override encrypted values only per provider, only when explicitly enabled in an unpackaged build.
- Settings UX restructured (2026-07-25): Settings is now a viewport-bounded tabbed dialog (General, AI, Metadata & Tags) with scrollable content and a persistent Cancel/Save footer. Archive maintenance actions moved to the View menu and command palette: tag dictionary, all-archive embeddings, confidence annotation, and health checks. Detailed non-secret operational information is in the separate View → Application Status dialog. Manual regression check: resize the app to its minimum height, open Settings, switch tabs with keyboard focus, scroll each tab, and confirm Cancel/Save remain reachable.

- `npm run build` runs `vue-tsc --noEmit && vite build`; type errors can block config verification.
- Use `npx vite build` to validate Vite/Rollup config behavior independently of TS typecheck failures.
- Vite CJS deprecation warning in this repo was resolved by renaming `vite.config.ts`/`vite.config.server.ts` to `.mts` and updating script references.
- QA creation IDs were minute-granularity and caused collisions during rapid create flows; switched to second+millisecond IDs with filesystem collision check.
- UX preference for this repo: optimize QA entry for high-throughput streams (keep primary create actions always visible and minimize scrolling/click friction).
- Electron Playwright coverage uses a temporary isolated working directory in `tests/e2e/electron.fixture.ts` so e2e runs do not write `threads.json` or `archive/` into the repo root.
- Playwright usage is now intentionally split between stable npm scripts in `package.json` (`test:e2e`, `test:e2e:debug`, `test:e2e:ui`, `test:e2e:report`) and explicit ad hoc CLI examples in `README.md` and `BUILD.md` for single-spec and filtered runs.
- When a documented Playwright command becomes a reusable repo-specific workflow, prefer a checked-in helper under `scripts/` over leaving the exact command line only in documentation. Current helper: `scripts/playwright-electron.sh`.
- Visual snapshot maintenance now has explicit scripts: `test:visual` to run visual regression checks and `test:visual:update` to accept the current UI as the new approved baseline when that change is intentional.
- Documentation was consolidated so README.md is now the canonical user/developer guide; BUILD.md is a short pointer to avoid duplicated instructions drifting.
- LLM Lens is now an optional, default-disabled feature (2026-07-25). `lensEnabled` is persisted in settings; when disabled, the panel, toolbar button, command-palette command, native View-menu entry, and reserved bottom space are absent. README is the canonical Lens usage guide; `USAGE.md` links to it rather than duplicating Lens instructions.
- LLM Lens design and usage review completed (2026-07-25): `doc/plans/LLM_Lens_Design_Usage_Review_2026-07-25.md` records purpose, current retrieval architecture, actual OpenAI-only operational requirement, documentation defects, and prioritized improvements. This review did not change application behavior.
- Export / Import feature added (2026-03-15): format logic is split from file transport in `electron/services/qaExportFormatService.ts`, `qaImportFormatService.ts`, `fileExportService.ts`, `fileImportService.ts` so future non-file targets (OneNote, etc.) can reuse the formatter/parser. IPC channels `export:qa`, `export:thread`, `import:file` added. UI entry points: Export button in QAContentPanel (selected QA), Export button per thread row in ThreadsPanel, keyboard shortcut `X` (export), `Ctrl+O` (import), and command palette entries. On import: IDs/timestamps are always regenerated (never overwrite); `original_id` and `original_timestamp` preserved as auxiliary fields; version mismatch uses best-effort with per-item warnings shown in toast + summary dialog.
- AI / LLM features merged (2026-03-xx from eveselov/dev branch post-fork): Added LLM Lens (InsightsPanel), archive health report (HealthReportDialog), AI metadata generation (topic, status, confidence, summary, relatedIds, concepts), vector embeddings + semantic search, confidence annotations (AnnotationDialog), and secrets management (API key storage). New services: `annotationService`, `embeddingService`, `healthService`, `insightsService`, `metadataService`, `secretsService`, `llm/openaiProvider`, `llm/providerFactory`, `llm/tokenTracker`, `llm/types`. New components: `AnnotationDialog.vue`, `HealthReportDialog.vue`, `InsightsPanel.vue`. New dependency: `openai@^6`. Global search (across archive, not just current thread) with scope toggle and save-as-thread added to QAListPanel + ThreadsPanel. `src/api-mock.ts` updated to include `llmProvider`, `llmModel` defaults and no-op stubs for all new AI API methods.
- Native Windows MSI packaging added with `npm run electron:build:msi`. The MSI is assisted, per-user, and uses pinned upgrade code `70DA883D-10E1-456D-BFBB-6E77AAE20DFC`; signing and per-machine deployment are deferred. New Windows archives default to an existing OneDrive root under `Documents\LLM-Aggregator`, falling back to `%USERPROFILE%\Documents\LLM-Aggregator`; users can override this in Settings.
- AI settings model picker now uses provider/model catalog plumbing (`ai:listProviders`, `ai:listModels`) backed by `electron/services/llm/providerRegistry.ts` and `electron/services/llm/modelCatalogService.ts`. Phase 1 behavior: OpenAI models are fetched dynamically when an API key is available, then cached in `model-catalog-cache.json`; UI falls back to cached/static curated models with warning text when fetch fails. The provider registry is intended as the extension point for future OpenAI-compatible providers and local gateways.
- OpenAI settings UX follow-up: `ai:listModels` now accepts an optional in-memory OpenAI API key override so model discovery works immediately after typing a key in Settings (before Save). Settings also disables non-enabled providers in the provider picker and refreshes model catalog automatically after a successful connection test.
- Anthropic provider support implemented: added `electron/services/llm/anthropicProvider.ts` and wired `providerFactory` to instantiate it when `llmProvider=anthropic`. Model catalog now supports dynamic Anthropic model discovery through `GET /v1/models`, including in-memory key override in `ai:listModels` so refresh works before saving settings. Settings exposes Anthropic as enabled provider and auto-refreshes model list after successful Anthropic connection test. Current limitation: embeddings and semantic-index generation remain OpenAI-only in this app provider path; UI now shows this explicitly and disables the embeddings generation button when Anthropic is selected.
- Secrets storage Phase 1 groundwork: settings schema now includes `allowDevEnvSecrets` and `devEnvSecretPrefix` (default `LLM_AGG_`), with development-only controls in `SettingsDialog.vue`. This is plumbing only: it introduces explicit, persisted intent for env-based secret override without changing existing secrets backend behavior yet. **Superseded** by the entry below — `devEnvSecretPrefix` was subsequently removed.
- Secrets storage V1 (Steps 1–5) implemented — full spec in `doc/plans/Secrets_Storage_Design_and_Implementation.md`. API keys are no longer stored in clear text: `electron/services/secrets/` adds a `SecretBackend` chain (`env` dev override → `safe-storage`), a resolver with fixed precedence, and a status projection that gives the renderer presence/masked-preview/provenance instead of key values. Electron `safeStorage` replaces the plan's separate keychain + encrypted-file tiers (OS holds the key; no native dependency; stays synchronous so the 11 `getProvider()` call sites are unchanged). `secrets:load` now returns `SecretsStatus`, `secrets:save` takes a partial update, and `secrets:recheck` / `secrets:devEnvVarNames` are new. `devEnvSecretPrefix` was dropped — the prefix is the fixed constant `LLM_AGG_`. The env override requires both the setting and `!app.isPackaged`. Legacy plaintext `secrets.json` is **not read**; it is renamed to `secrets.json.orphaned.bak` on startup and users re-enter keys. Step 6 (true migration) is deliberately deferred. Also fixed while in the file: unguarded model-catalog cache write, an unreachable Anthropic branch, and the dual-key model-discovery override collapsed to one scoped `apiKeyOverride`. 28 unit tests added.
